
<div class = "container">
	<h2 class="center-block title-1 title-1-about-us " style="float: none">Фотогалерея</h2>
	<% @pictures.each do |picture| %>
   <div>
     <%= link_to image_tag(picture.image.url),
				picture.image.url, { :class => "fancybox", :rel => "gallery1" } %>
	</div>
	<% end %>
  </div>



