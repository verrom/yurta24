<h1><%= t('article.edit') %></h1>
 
<%= form_for :description, url: description_path(@description), method: :patch do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text, :class => "tinymce", :rows => 15, :cols => 80 %>
  </p>
  <%= f.submit "Сохранить" %>
<% end %>
 
<%= link_to 'Назад', manager_path %>
<%= tinymce %>